version: '0.1'
directories:
  config:
    path: /etc/clvm
  dotconf:
    path: /etc/clvm/clvm.conf
  log:
    path: /var/log/clvm
  data:
    path: /var/lib/clvm
  lib:
    path: /usr/lib/clvm
files:
  config:
    path: /etc/clvm/clvm.conf
  log:
    path: /var/log/clvm/clvm.log
  pid:
    path: /var/run/clvm.pid
packages:
  default:
    name: clvm
services:
  default:
    name: clvm
processes:
  default:
    path: /usr/sbin/clvmd
